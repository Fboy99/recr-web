/** @type {import('next').NextConfig} */
const nextConfig = {
    // reactStrictMode: true,
    //     images: {
    //         domains: ['images.ctfassets.net'], 
    //         formats: [], 
    //         deviceSizes: [320, 420, 768, 1024, 1200], // Common device sizes
    //         imageSizes: [16, 32, 48, 64, 96], // Custom image sizes
           
    //   },
};

export default nextConfig;
